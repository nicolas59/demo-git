pipeline {
    agent any

    stages {
        stage('Prepare') {
            steps {
                git url:'https://github.com/nicolas59/epsi-mspr-data', branch:'main' 
            }
        }
        stage('Display folder') {
            steps {
                echo '#### ##### Display folder ########'
                sh '''
                ls -lrt
                ls -lrt data
                ''' 
            }
        }
    }
}
